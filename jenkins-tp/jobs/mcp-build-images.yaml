- job-template:
    name: mcp-build-images-latest
    description: |
      Build MCP Docker images and store them into Docker registry with latest tag.
    node: standard
    builders:
      - shell:
          !include-raw 'builders/mcp-build-images.sh --tag latest'
    concurrent: false
    scm:
      - openstack:
          scm-basedir: ''
          scm-branch: 'master'
          scm-repo: 'openstack/fuel-ccp'
    wrappers:
      - ng-cleanup

- job-template:
    name: mcp-build-images-build-number
    description: |
      Build MCP Docker images and store them into Docker registry with BUILD_NUMBER as a tag.
    node: standard
    builders:
      - shell:
          !include-raw 'builders/mcp-build-images.sh --tag $BUILD_NUMBER --purge 5'
    concurrent: true
    scm:
      - openstack:
          scm-basedir: ''
          scm-branch: 'master'
          scm-repo: 'openstack/fuel-ccp'
    wrappers:
      - ng-cleanup


- project:
    name: Build MCP Docker images and store them into Docker registry.
    jobs:
       - 'mcp-build-images-latest'
       - 'mcp-build-images-build-number'
